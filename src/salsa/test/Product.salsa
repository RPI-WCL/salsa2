module salsa.test;
import java.lang.System;
import java.lang.Integer;
import java.util.Date;

immutable class Product {
    String name;
    Date produceDate;
    Date expireDate; 

    Product(String name) {
        this.name = name;
    }

    String getName() {
        return name;
    }

    Date getProduceDate() {
        System.out.println(produceDate.toString());
        if (produceDate != null) {
            int date = produceDate.getDate();
        }
        return produceDate;
    }
}
